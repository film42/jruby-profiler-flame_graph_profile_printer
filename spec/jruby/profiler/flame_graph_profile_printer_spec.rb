require 'spec_helper'

describe Jruby::Profiler::FlameGraphProfilePrinter do
  it 'has a version number' do
    expect(Jruby::Profiler::FlameGraphProfilePrinter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
